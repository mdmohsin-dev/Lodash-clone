# sortedUniq

Creates a duplicate-free version of a sorted array.

## Parameters

- `array` (Array): The sorted array to inspect  

## Returns

- (Array): Returns the new array of unique values

## Example

```js
sortedUniq([1, 1, 2, 2, 3, 4]);

ouput
[1, 2, 3, 4]