# lastIndexOf

Gets the index of the last occurrence of a value in an array.

## Parameters

- `array` (Array): The array to inspect  
- `value` (*): The value to search for  

## Returns

- (number): Returns the index of the matched value, else `-1`

## Example

```js
lastIndexOf([1, 2, 3, 2], 2);

output
3