# fromPairs()

Converts an array of key-value pairs into an object.

## Parameters
- `pairs` (Array): An array of `[key, value]` pairs  

## Example
```js
fromPairs([["a", 1], ["b", 2]]);

output
{ a: 1, b: 2 }