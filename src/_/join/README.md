# join

Creates a string by joining all elements of an array with a separator.

## Parameters

- `array` (Array): The array to process  
- `separator` (string): The value to separate each element (default: `","`)

## Returns

- (string): Returns the joined string

## Example

```js
join(["a", "b", "c"], "_");

outpu
'a_b_c'