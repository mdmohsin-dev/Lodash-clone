# concat()

Creates a new array by combining the given array with additional arrays and/or values.

## Parameters
- `array` (Array): The original array  
- `...values` (Array | any): Arrays or values to concatenate  

## Example
```js
concat([1, 2], [3, 4], 5);

output
[1, 2, 3, 4, 5]