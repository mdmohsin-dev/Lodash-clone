# reverse

Reverses an array in place.

## Parameters

- `array` (Array): The array to modify  

## Returns

- (Array): Returns the reversed array

## Example

```js
reverse([1, 2, 3]);

output
[3,2,1]