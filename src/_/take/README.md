# take

Creates a slice of an array with `n` elements taken from the beginning.

## Parameters

- `array` (Array): The array to query  
- `n` (number): The number of elements to take  

## Returns

- (Array): Returns the new array of taken elements

## Example

```js
take([1, 2, 3], 2);

output
[1,2]