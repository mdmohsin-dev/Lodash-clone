# mean()

Calculates the average (mean) of all numbers in an array.

## Parameters

- `array` (Array): The array of numbers  

## Returns

- (number): Returns the average value  

## Example

```js
mean([1, 2, 3, 4]); => 2.5

mean([10, 20]); => 15

mean([]); => NaN