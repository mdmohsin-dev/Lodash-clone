# pullAt

Removes elements from an array at given indexes and returns the removed elements.

## Parameters

- `array` (Array): The array to modify  
- `indexes` (Array): The indexes of elements to remove  

## Returns

- (Array): Returns an array of removed elements

## Example

```js
pullAt(['a','b',3,4,5],[1,3]);

output
['b',4]
