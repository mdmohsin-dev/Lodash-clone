# tail

Gets all elements of an array except the first one.

## Parameters

- `array` (Array): The array to query  

## Returns

- (Array): Returns the new array excluding the first element

## Example

```js
tail([1, 2, 3, 4]);

output
[2, 3, 4]