# isBoolean()

Checks if a value is a boolean.

## Parameters

- `value` (*): The value to check  

## Returns

- (boolean): Returns `true` if the value is a boolean, else `false`

## Example

```js
isBoolean(true); => true
isBoolean(false); true
isBoolean(0); => false