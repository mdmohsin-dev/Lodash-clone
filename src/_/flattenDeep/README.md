# flattenDeep()

Recursively flattens an array into a single-level array.

## Parameters
- `array` (Array): The array to flatten  

## Example
```js
flattenDeep([1, [2, [3, [4]], 5]]);

output
[1, 2, 3, 4, 5]