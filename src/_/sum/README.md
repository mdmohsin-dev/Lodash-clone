# sum()

Calculates the sum of all numbers in an array.

## Parameters

- `array` (Array): The array of numbers to calculate the sum  

## Returns

- (number): Returns the total sum  

## Example

```js
sum([1, 2, 3, 4]); => 10

sum([5, 10, -5]); => 10