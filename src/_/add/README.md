# add

Adds two numbers and returns the result.

## Parameters

- `augend` (number): The first number  
- `addend` (number): The second number  

## Returns

- (number): Returns the sum  

## Example

```js
add(2, 3); => 5

add(10, -4); => 6

add(0, 0); => 0