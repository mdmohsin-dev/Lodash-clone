# union

Creates a new array of unique values by combining multiple arrays.

## Parameters

- `arrays` (...Array): The arrays to inspect  

## Returns

- (Array): Returns the new array of combined unique values

## Example

```js
union([1, 2], [2, 3], [3, 4]);

output
[1,2,3,4]