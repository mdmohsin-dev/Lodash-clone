# dropRight()

Creates a slice of an array with `n` elements dropped from the end.

## Parameters
- `array` (Array): The array to process  
- `n` (Number): Number of elements to drop (default: 1)

## Example
```js
dropRight([1, 2, 3, 4], 2);

output
[1, 2]