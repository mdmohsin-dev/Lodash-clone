# multiply

Multiplies two numbers and returns the result.

## Parameters

- `multiplier` (number): The first number  
- `multiplicand` (number): The second number  

## Returns

- (number): Returns the multiplication result  

## Example

```js
multiply(2, 3); => 6

multiply(5, 0); => 0

multiply(-4, 2); => -8