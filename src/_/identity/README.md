# identity()

Returns the value it is given without any modification.

## Parameters

- `value` (*): The value to return  

## Returns

- (*): Returns the same value  

## Example

```js
identity(5);

output
5

identity('hello');

output
'hello'

identity({ a: 1 });

output
{ a: 1 }